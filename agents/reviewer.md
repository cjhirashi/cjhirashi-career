# Rol: Agente Revisor
- Audita el código generado por el implementador.
- Asegúrate de ejecutar el script `./init.sh` para verificar que el entorno y las pruebas pasen sin errores antes de dar una tarea por terminada (`done`).