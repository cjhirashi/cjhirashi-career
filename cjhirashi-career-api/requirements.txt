# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Caching, Queues, Rate Limiting (FASE 1+)
redis>=5.0.0

# Object Storage (MinIO / S3-compatible)
minio==7.2.15
Pillow==10.4.0

# Database
sqlalchemy==2.0.23
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.12.1

# Validation & Settings
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0
python-dotenv==1.0.0

# Security & Authentication
pyjwt==2.8.0
bcrypt==4.1.2
cryptography==41.0.7

# Agent Bedrock (chat + embeddings) and its Qdrant knowledge base.
# boto3/botocore pinned to a version with cross-region inference profile
# support (GetInferenceProfile etc.) - 1.34.162 predates it and silently
# lacks those operations (AttributeError, not a clean "not supported" error).
boto3==1.43.76
botocore==1.43.76
qdrant-client==1.11.3

# PDF generation (WeasyPrint — in-process, see services/pdf/)
weasyprint==69.0
markdown==3.6

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1
httpx==0.25.2

# Development
python-dateutil==2.8.2
pytz==2023.3
