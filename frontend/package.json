{
  "name": "mcp-frontend-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend web para MCP Tools Server. Interfaz para generar y descargar documentos PDF (CVs, Cover Letters) via el servidor MCP.",
  "scripts": {
    "dev": "echo \"TODO: configurar script de desarrollo\" && exit 1",
    "build": "echo \"TODO: configurar script de build\" && exit 1",
    "start": "echo \"TODO: configurar script de arranque\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {}
}
